import {KeyakiSchedule} from '../src/keyakiSchedule';

describe('setSchedule', (): void => {
    test('should say hello to Tom.', (): void => {
        const keyakiSchedule = new KeyakiSchedule();
        expect(1).toBe(1);
    });
});
